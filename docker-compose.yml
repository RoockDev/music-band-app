services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: banda
      POSTGRES_USER: banda
      POSTGRES_PASSWORD: banda
    ports:
      - "5432:5432"
    volumes:
      - banda_postgres_data:/var/lib/postgresql/data

volumes:
  banda_postgres_data:
